@model RiskPulse.Models.ViewModel.AssessmentWizardViewModel

<section class="wizard-step-panel" data-step="3" style="display:none;">
    <div class="card-stasis p-3">
        <h6 class="font-display-md mb-1">Step 3 — KRI template</h6>
        <p class="font-body-sm mb-3" style="color: var(--on-surface-variant);">
            Pick the key risk indicator template that will be tracked for this assessment.
        </p>
        <div class="row">
            <div class="col-lg-6">
                <label class="input-label" for="wizardKriId">KRI Template</label>
                <select id="wizardKriId" class="input-stasis">
                    <option value="0">Select a KRI template...</option>
                    @foreach (var opt in Model.KriOptions)
                    {
                        <option value="@opt.Value" selected="@(Model.KriHeaderId == opt.Value ? "selected" : null)">@opt.Label</option>
                    }
                </select>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-between gap-2 mt-3">
        <button type="button" class="btn-stasis-secondary wizard-back-btn"><i class="fas fa-arrow-left"></i> Back</button>
        <button type="button" class="btn-stasis-primary wizard-next-btn">Next <i class="fas fa-arrow-right"></i></button>
    </div>
</section>
